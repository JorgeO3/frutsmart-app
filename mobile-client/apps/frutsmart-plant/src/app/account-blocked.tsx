import { Text } from "react-native";
import AppView from "../components/AppView";

export default function AccountBlocked() {
  return (
    <AppView
      style={{ flex: 1, justifyContent: "center", alignItems: "center" }}
    >
      <Text>AccountBlocked</Text>
    </AppView>
  );
}
