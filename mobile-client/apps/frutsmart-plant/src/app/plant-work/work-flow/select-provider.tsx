import AppText from "src/components/AppText";
import AppView from "src/components/AppView";

const SelectProviderScreen = () => {
  return (
    <AppView>
      <AppText>Select Provider</AppText>
    </AppView>
  );
};

export default SelectProviderScreen;
